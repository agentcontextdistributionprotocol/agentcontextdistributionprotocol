# Changelog

All notable changes to ACDP are recorded here. ACDP follows the versioning policy in [VERSIONING.md](VERSIONING.md).

## v0.3.0 — (Draft) — 2026-07-04

**Backward-compatible minor version.** Every existing `v0.1.0`/`v0.2.0` body, signature, `content_hash`, and RFC-ACDP-0010 receipt remains valid and wire-compatible: no body field, JCS rule, content-hash semantic, signature semantic, or receipt semantic changed, and no schema `$id` changed (additive edits in the `v0.1.0` namespace per VERSIONING.md — one additive field on the closed `capabilities.limits` object, one new closed `lineage_head_receipt` schema, one optional member on the open retrieval envelope). The 0.3.0 surface is **Draft** — RFC-ACDP-0011's `Version:` header reads `0.3.0-draft`, amended passages in RFC-0003/0004/0007/0008 are marked *(0.3.0)*, and the line goes Final once the 0.3.0 conformance fixtures pass against two independent implementations. Unlike 0.2.0's publish-response caveat, 0.3.0 changes **no existing parse surface**: the new retrieval member rides the envelope's top-level openness (RFC-ACDP-0001 §6), and the capabilities field is optional inside a closed object that 0.3.0 verifiers know.

- **RFC-ACDP-0011 Lineage-Head Receipts (new, Draft) — the first 0.3.0 item.** RFC-ACDP-0010 receipts bind *publish-time* facts; nothing in 0.2.0 attests *current-ness* — a registry can serve a stale `status: active` or a stale `/current` head with no signed evidence contradicting it (RFC-ACDP-0004 §4 status is registry-derived; RFC-ACDP-0010 §13 concedes serve-time honesty is out of scope). The lineage-head receipt is the registry's signed, timestamped serve-time claim: *"as of `as_of`, the head of `lineage_id` is `head_ctx_id` at `head_version` with `head_status`."* Closed object (`receipt_version: "acdp-lhr/1"` doubling as an in-preimage domain separator, `registry_did`, `lineage_id`, `head_ctx_id`, `head_version`, `head_status`, `as_of`, `signature`); signing reuses RFC-ACDP-0010 §5 verbatim (JCS preimage minus `signature` → SHA-256 → sign the ASCII `"sha256:<hex>"` string) under the **same** registry receipt signing key and §9 key lifecycle — no new key role. Issuance: REQUIRED on `GET /lineages/{lineage_id}/current` when the profile is advertised, MAY accompany full retrieval, NEVER on body-only retrieval; `as_of` is the registry's millisecond-truncated response time; heads are requester-relative under lineage visibility (RFC-ACDP-0004 §5.4). Verification is a six-step cross-checking procedure (closed parse; signature under the registry DID's key; authority binding; lineage binding; head byte-match against the served response, with a self-contradiction rule for non-head full retrievals; future-`as_of` skew rejection). Freshness is consumer policy: the receipt proves the claim was made at `as_of`, never future validity — replayed receipts are bounded by their age; RECOMMENDED max-age 300 s, skew allowance 120 s. Honest scope: stale-head serving is detected only comparatively (a fresher receipt or a cross-check); a registry lying consistently to everyone is the job of the RFC-ACDP-0009 §2.11 transparency log, of which this is the cheap complement, not the replacement; split-view between equally authorized consumers becomes non-repudiable signed evidence (the RFC-ACDP-0010 §13 property, one layer up). Status note: RFC-ACDP-0011 promotes **no** RFC-ACDP-0009 reservation — `lineage_head_receipt` was never reserved; the RFC extends RFC-ACDP-0010.
- **Profile (`registries/profiles.md` + `profiles.json`).** New `acdp-registry-head-receipts` (0.3.0, Draft; prerequisite `acdp-registry-receipts` — same signing key and DID plumbing; requires `acdp_version` ≥ 0.3.0); `acdp-consumer` gains the `lhr-*` fixtures as a conditional block (required only for consumers that rely on `lineage_head_receipt`; ignoring the member under the unknown-field rule remains conformant). RFC-ACDP-0001 §9.1 gains the profile's MUST list per the registry's "Adding a profile" rule.
- **Error surface: no change.** Head-receipt verification failures reuse `invalid_receipt` (RFC-ACDP-0010 §11); RFC-ACDP-0011 §9 states explicitly that no new wire code is introduced (`registries/error-codes.md` row annotated). No schema enum change.
- **Schemas (additive, `v0.1.0` namespace retained).** New `acdp-lineage-head-receipt.schema.json` (closed, `receipt_version` const-pinned); `acdp-context.schema.json` gains the optional `lineage_head_receipt` member (the envelope was already open at the top level — no parse-surface change).
- **Conformance pack (4 new fixtures + runner support).** Executed arithmetically: `lhr-001` (head-receipt golden vector over the `sig-001` lineage, signed by the `rcpt-001` registry test keypair; the suite now reports 44 arithmetic vectors). Behavioral: `lhr-002` (stale-head mismatch — receipt attests v1, response serves v2), `lhr-003` (registry-DID mismatch), `lhr-004` (future `as_of` beyond skew, validly signed). New `lhr` fixture family registered; `scripts/conformance-runner.py` executes keypair-bearing `lhr-*` fixtures exactly as it executes `rcpt-001`; vectors generated by `temp/gen-0.3.0-vectors.py` (following the 0.2.0 generator).
- **RFC-ACDP-0004 §5.2.** One *(0.3.0)* sentence cross-references the `lineage_head_receipt` member on the `/current` response.
- **Idempotency-Key REQUIRED for `acdp-registry-core` at `acdp_version` ≥ 0.3.0 (RFC-ACDP-0003 §6.4, new; RFC-ACDP-0007 §3.2, §3.5 item 10).** A registry advertising `acdp_version` ≥ 0.3.0 MUST support the `Idempotency-Key` header on `POST /contexts`: `supports_idempotency_key` MUST be `true` and MUST still be advertised for introspection; the §6.2.2 atomic storage contract MUST be implemented (reference pattern: storage-level `UNIQUE(agent_id, idempotency_key)` with transactional insert); and the §6 TTL bounds (86400–604800 s, advertised as `limits.idempotency_key_ttl_seconds`) MUST be enforced. A registry whose storage cannot provide single-writer/CAS atomicity MUST NOT advertise `acdp_version` ≥ 0.3.0. Rationale: without it, duplicate publication under retry is the protocol's *default* behavior — a producer whose POST times out after persistence mints a second `ctx_id` on retry, and every producer must carry client-side dedup forever; the registry-side cost is one uniqueness constraint. Conformance tightening per VERSIONING.md, not a wire change: a 0.2.0-conformant registry upgrades by implementing §6 before advertising 0.3.0. New behavioral fixture `idem-007-required-at-0-3-0.json` pins the capabilities-level rejection (`acdp_version` ≥ 0.3.0 with `supports_idempotency_key` absent or `false` → `schema_violation`, consumer MUST NOT proceed); `idem-001..005` become unconditionally required for 0.3.0 registries. 0.1.0/0.2.0 registries: unchanged (optional, capability-gated).
- **`limits.max_publish_per_minute` OPTIONAL capabilities field (RFC-ACDP-0007 §3.2, §3.5 item 11; RFC-ACDP-0008 §4.3; `acdp-capabilities.schema.json`).** New OPTIONAL integer ≥ 1: the nominal per-agent publish ceiling the registry enforces on `POST /contexts`, advisory for producer pacing. Its presence does NOT change the RFC-ACDP-0008 §4.3 MUSTs — per-agent rate limiting is required regardless, and 429 responses MUST still carry `Retry-After` (mandatory since round 5); registries SHOULD advertise the ceiling. Schema edit: the closed `limits` sub-object (`additionalProperties: false`, retained) gains the field in the `v0.1.0` namespace — the same additive-edit posture as `registry_receipt` on the closed publish response in 0.2.0; strict pre-0.3.0 validators of `limits` must track the edit. New fixture `caps-007-max-publish-per-minute.json` (accept valid doc; reject zero/negative/non-integer as `schema_violation`). This lands the ceiling-advertisement deferred to 0.3.0 by the round-5 rate-limit clarification.
- **Conformance manifest refresh (`registries/profiles.json`, `registries/profiles.md`, `schemas/conformance/README.md`).** `caps-007` and `idem-007` are wired into the `acdp-registry-core` and `acdp-consumer` profiles and the fixture index; the `acdp-registry-core` idempotency conditional now records that `idem-001..005` are unconditional at `acdp_version` ≥ 0.3.0.
## v0.1.0 — Clarifications addendum (round 5) — 2026-07-04

**Non-breaking specification clarifications, a new conformance fixture, and a new non-normative document.** The RFCs remain `v0.1.0 Final`; this round tightens two observable-behavior contracts found during evaluator review and documents the data-protection posture. No body field, schema `$id`, JCS rule, content-hash semantic, or signature semantic changed — every existing `v0.1.0` body, signature, and `content_hash` remains valid and wire-compatible. No JSON Schema changed.

- **Partial-walk reporting (RFC-ACDP-0006 §4.1).** When any of the four traversal controls (max depth, max total nodes, max fanout per context, total walk timeout) truncates a `derived_from` walk and the implementation surfaces a partial result rather than aborting, it MUST distinguish that truncated result from a complete one (e.g. a `complete: false` flag or explicit truncation marker in its API result) and MUST NOT present a truncated evidence set as exhaustive; the marker SHOULD identify which control fired and, where practical, the unwalked frontier. Aborting the whole walk with `cross_registry_resolution_failed` remains conformant (and remains preferred for fanout overruns). Rationale: walks exist to assemble the evidence chain behind a conclusion (§2) — silent truncation misreads as "the walk covered everything" and converts a resource-bounding control into an integrity failure. New behavioral fixture `fed-010-truncated-walk-reported.json` pins the success-with-truncation-marker path; wired into the `acdp-registry-federated` and `acdp-consumer` profiles.
- **Rate-limit observable contract (RFC-ACDP-0008 §4.3, RFC-ACDP-0007 §6).** The `Retry-After` header on 429 `rate_limited` responses is upgraded from SHOULD-when-bounded to MUST (integer seconds or HTTP-date, RFC 7231 §7.1.3); the error code and envelope were already MUST. A limiter that cannot compute an exact refill horizon (e.g. concurrency-based) MUST emit a conservative estimate rather than omit the header — it is the only machine-readable backoff signal the protocol defines. The existing acknowledgment that the *trigger* is black-box-untestable is retained: `rate-001` pins the wire shape (now including mandatory `Retry-After`) and implementers MUST self-test per its recipe. No capabilities field is added — the `limits` object is a closed schema, and advertising rate limits there is deferred to 0.3.0. Consumers still tolerate a missing header from pre-round-5 registries and fall back to their own backoff.
- **Data-protection posture documented (`docs/data-protection.md`, new; non-normative).** The erasure/GDPR consequences of permanence in one place: bodies are permanent and unerasable by design (non-goals §5/§13), so (a) producers SHOULD carry personal or sensitive data by reference (`data_refs[].location` into an erasable system of record) rather than embedded or in search-indexed fields; (b) supersession-with-narrower-audience is the remediation for over-shared data, and what it does NOT remediate is made explicit (the original stays retrievable to its original audience — it is a correction signal, not an un-publish); (c) retraction is reserved (RFC-ACDP-0009 §2.1) and is mark-not-delete by design intent; (d) a deployment-policy hard delete (legal hold) is an out-of-protocol override that breaks lineage verifiability for downstream deriveds and MUST be surfaced to consumers as an explicit policy removal, never as silent absence (broken chain ≠ silent absence). Cross-linked from `docs/non-goals.md` items 5 and 13 and, as a "See also", from RFC-ACDP-0002 §6.4; listed in the README repository map.
- **`private` + `audience` worked example (`docs/integration-guide.md`, `examples/visibility/private-with-audience-body.json`, new).** A concrete two-agent walkthrough of the retrieval/search asymmetry (RFC-ACDP-0002 §7, RFC-ACDP-0005 §2.5.5): a DID in the `audience` of a `private` context can RETRIEVE it given the `ctx_id` but can never DISCOVER it via keyword search or the `derived_from` filter, so the `ctx_id` must travel out-of-band (a message, or a discoverable pointer context). The failure mode is named — an audience member searches, finds nothing, and wrongly concludes the context does not exist — with the guidance that search-locating consumers need `visibility: restricted` instead. The new schema-validated example follows the existing `examples/visibility/` naming pattern.
- **Conformance manifest refresh (`registries/profiles.json`, `registries/profiles.md`, `schemas/conformance/README.md`).** `fed-010` is wired into the `acdp-registry-federated` and `acdp-consumer` profiles and the fixture index; the `fed` family description now covers §4.1 traversal-control behavior alongside §7 SSRF; the `rate-001` row and profile note reflect the mandatory `Retry-After` header.

## v0.2.0 — Trust & Hardening (Draft) — 2026-06-12

**Backward-compatible minor version — the first specification change beyond v0.1.0.** Every existing `v0.1.0` body, signature, and `content_hash` remains valid and wire-compatible: no body field, JCS rule, content-hash semantic, or signature semantic changed, and no schema `$id` changed (per VERSIONING.md, a non-breaking minor stays in the `v0.1.0` schema namespace with additive edits). The 0.2.0 surface is **Draft** — amended passages are marked *(0.2.0)* in-prose, `Version:` headers of touched RFCs read `0.2.0-draft`, and the line goes Final once the new conformance pack passes against two independent implementations. The one parse-surface caveat: the previously closed publish-response shape gains the OPTIONAL `registry_receipt` member, so strict v0.1.0 publish-response decoders must be upgraded before publishing to receipts-advertising registries (migration note in RFC-ACDP-0010 §12).

- **RFC-ACDP-0010 Registry Receipts (new, Draft).** Promotes the RFC-ACDP-0009 §2.7 reservation to a full normative RFC, closing the registry-honesty gap (RFC-ACDP-0008 §9.1) for receipt-bearing responses. The receipt binds `ctx_id`, `lineage_id`, `origin_registry`, `created_at`, the body `content_hash`, and the resolved producer-key fingerprint to the registry's DID. Signing reuses the producer construction exactly (JCS preimage minus `signature` → SHA-256 → sign the ASCII `"sha256:<hex>"` string). `key_fingerprint` is pinned byte-for-byte: `"sha256:" + lowercase_hex(SHA-256(raw key bytes))` over the 32-byte raw Ed25519 key or 33-byte SEC1-compressed P-256 point. Receipts are minted atomically with persistence, returned on the publish response and full retrieval (never the body-only endpoint), and verified by a six-step cross-checking procedure. Receipt-key lifecycle: retired keys stay in `verificationMethod` indefinitely; rotation removes from `assertionMethod` only. Honest scope: receipts make registry claims attributable and non-repudiable, not unforgeable at mint time — the transparency log is reserved as RFC-ACDP-0009 §2.11.
- **Historical key validity (RFC-ACDP-0010 §10, RFC-ACDP-0001 §5.11).** Producers SHOULD retain rotated keys in `verificationMethod` (removing from `assertionMethod` only). With a verified receipt attesting the old key's fingerprint, historical contexts verify with the distinguishable status *historically authorized (receipt-attested)*; without a receipt, v0.1.0 behavior is unchanged (fail closed under the strict profile). Fixture `rot-001` pins both halves plus the compromise-revocation case.
- **did:key producers (RFC-ACDP-0001 §5.4 / §5.11.1, RFC-ACDP-0007 §3.1).** `agent_id` MAY be `did:key:z<base58btc>` where the registry advertises `"did:key"` in `supported_did_methods`; `signature.key_id` is `did:key:z<mb>#z<mb>`. Resolution is pure — multibase decode, multicodec prefix check (`0xed01` for Ed25519, `0x8024` for P-256 — the unsigned-varint encodings of codes `0xed` and `0x1200`), no network, no DID document, no `assertionMethod` check. Non-advertising registries reject did:key publishes with `key_resolution_failed` (pinned). `registry_did` stays did:web-only. A non-normative two-tier identity appendix (org anchors on did:web, ephemeral/archival producers on did:key) is RFC-ACDP-0001 Appendix B.
- **Explicit `acdp_version` for 0.2.0 producers (RFC-ACDP-0001 §6).** 0.2.0 publish requests MUST carry `acdp_version` explicitly; the omitted form remains valid on the wire from 0.1.0 producers indefinitely (no retroactive hash change — `can-012` pins omitted/explicit-`0.1.0`/explicit-`0.2.0` as three distinct preimages).
- **Hash-divergence corpus (RFC-ACDP-0001 Appendix A, `can-012`).** A non-normative SDK implementers' checklist enumerating all 13 known divergence sources (timestamps, negative zero, numeric boundaries, empty-vs-absent, null members, UTF-8, unknown-field preservation, exclusion-by-name, `acdp_version` forms, DataRef openness, signing-input framing, fingerprint encoding) with their fixture IDs; `can-012` adds the previously unpinned vectors.
- **Lineage anchoring (RFC-ACDP-0001 §5.6.2, RFC-ACDP-0003 §2.1 step 9).** Registries MAY validate a v(N+1) publish against the persisted `lineage_id`/`version` of the immediate predecessor, demoting the full walk-back to an integrity audit — removing the `lineage_walk_failed` liveness failure when a deep intermediate is unretrievable while the immediate predecessor exists.
- **Idempotency tightening (RFC-ACDP-0003 §6.2).** Keys are explicitly scoped per `agent_id` (verified signing identity): two agents using the same key never interact. Non-normative storage guidance: unique constraint on `(agent_id, idempotency_key)` with transactional insert and TTL cleanup; registries without single-writer/CAS storage MUST NOT advertise `supports_idempotency_key`.
- **Error surface (RFC-ACDP-0007 §5, `registries/error-codes.md`, `acdp-error.schema.json`).** New code `invalid_receipt` (502 on the wire from federated resolvers about upstream receipts; the consumer-side verification-failure category otherwise). Decision recorded: no `receipt_unavailable` — receipts-profile registries MUST always mint.
- **Security model re-baselined (RFC-ACDP-0008).** §9.1–§9.3 rewritten: the registry-honesty and historical-key limitations stand for receipt-less responses and are closed where a verified receipt is present; new assurance-level table (registry attribution, identifier binding, and key-at-publish-time move Low → High with a verified receipt; timestamp authenticity Low → Medium pending the transparency log); threat table gains row 3.11; §7.3 gains the receipt-attested rotation path.
- **Schemas (additive, `v0.1.0` namespace retained).** New `acdp-registry-receipt.schema.json` (closed); `acdp-context.schema.json` and `acdp-publish-response.schema.json` gain the optional `registry_receipt` member; error enum gains `invalid_receipt`; openness map (RFC-ACDP-0007 §3.3.1) gains the receipt row.
- **Conformance pack (13 new fixtures + runner support).** Executed arithmetically: `rcpt-001` (receipt golden), `fp-001` (fingerprint vectors), `sig-003` (did:key golden — the plan's `sig-002` name was already taken by the ECDSA-P256 golden), `can-012` (divergence corpus), plus a new end-to-end check of `examples/retrieval/golden-context-with-receipt.json`; the suite now reports 43 arithmetic vectors. Behavioral: `rcpt-002`/`003`/`004`, `rot-001`, `dk-001`..`dk-004`, `fed-009`. New fixture families `rcpt`, `fp`, `rot`, `dk` registered; `registries/profiles.json` + `registries/profiles.md` gain the `acdp-registry-receipts` profile and the did:key conditionals; `scripts/conformance-runner.py` executes the new `rcpt-`/`fp-` families and derives did:key identities (base58-btc) for `sig-*` fixtures.
- **RFC-ACDP-0009.** §2.7 marked promoted to RFC-ACDP-0010 (the v0.1.0 preserve-verbatim guidance is retained verbatim for 0.1.0-pinned libraries); new §2.11 reserves the append-only registry-signed transparency log (Merkle checkpoints) with reserved field/endpoint/profile names only.
## v0.1.0 — Clarifications addendum (round 4) — 2026-06-10

**Editorial refresh of the project-governance and contributor documents — no normative or wire change.** The RFCs remain `v0.1.0 Final`. No body field, schema `$id`, JCS rule, content-hash semantic, or signature semantic changed — every existing `v0.1.0` body, signature, and `content_hash` remains valid and wire-compatible. No JSON Schema, RFC prose, registry entry, or conformance fixture changed.

- **Code of Conduct completed to the Contributor Covenant 2.1 it cites (`CODE_OF_CONDUCT.md`).** Added the Pledge, Scope, Enforcement Responsibilities, a private reporting path (consistent with `governance/GOVERNANCE.md`, not a public issue), and the four-tier Enforcement Guidelines consequence ladder. The document previously claimed adaptation from Covenant 2.1 without carrying its enforcement sections.
- **Contributor guide expanded (`CONTRIBUTING.md`).** Documented the three-file workflow for adding a conformance fixture (fixture JSON + both profile manifests + the README index) and the executed-vs-behavioral fixture distinction; added the missing `data-ref-types.md` registry and the `profiles.json`/`profiles.md` synchronization rule; added a versioning/releases section linking `VERSIONING.md`, `RELEASE.md`, and the changelog convention; added pull-request and security-reporting guidance; noted `make validate` as the CI gate.
- **README release wiring (`README.md`).** Added `RELEASE.md` to the repository map (it was present in the tree but unlisted), a "Releases and versioning" subsection, a wire-frozen note, and Code-of-Conduct / security-reporting pointers in the Contributing section.
- **Release-checklist and versioning cross-links (`RELEASE.md`, `VERSIONING.md`).** Broadened the stray-RC-reference grep to cover `docs/` and `manifesto/` prose with an explicit list of the dirs intentionally excluded; cross-linked `CONTRIBUTING.md`; added a `VERSIONING.md` → `RELEASE.md` back-reference to the promotion checklist.

## v0.1.0 — Clarifications addendum (round 3) — 2026-05-30

**Non-breaking specification clarifications and a new conformance fixture.** The RFCs remain `v0.1.0 Final`; this round records clarification/addendum items raised in an RFC follow-up review. No body field, schema `$id`, JCS rule, content-hash semantic, or signature semantic changed — every existing `v0.1.0` body, signature, and `content_hash` remains valid and wire-compatible. No JSON Schema changed.

- **Multi-tenancy implementation note (RFC-ACDP-0008 §6.4, new).** ACDP core defines no multi-tenant assignment — there is no protocol-level tenant identifier in the body, registry state, or capabilities, and no normative request-to-tenant mapping. Where a deployment partitions by tenant, the attribution MUST rest on an authenticated signal: implementations MUST NOT trust an unauthenticated tenant indicator (e.g. an `X-Tenant` header, hostname, or path segment) unless protected by deployment policy at a trust boundary, an authenticated gateway that stamps the tenant after authentication, or signed token claims. Treating an unauthenticated header as authoritative is a cross-tenant authorization bypass that the §4.5 visibility checks cannot catch, because they run *after* (forged) tenant attribution.
- **Cross-registry resolution is public-only in v0.1.0 (RFC-ACDP-0006 §4.4, new; §5 failure-mode row expanded).** v0.1.0 defines no mechanism to authenticate to a *remote* registry when following an `acdp://` reference — no bearer-token forwarding, token exchange, or delegated credential. A `restricted`/`private` context on a remote registry is therefore opaque (`not_found` / HTTP 404) to a cross-registry caller, who must authenticate to that registry directly, out of band. Authenticated remote retrieval (bearer-forwarding / token-exchange expectations and authority-trust rules) is out of scope for v0.1.0 and reserved for a future version (see RFC-ACDP-0009 §2.6).
- **Pagination MUST NOT terminate early on a fully-post-filtered page (RFC-ACDP-0005 §2.3).** Visibility scoping and other per-requester filters are applied after a storage page is read. A registry MUST NOT signal exhaustion (omit `next_cursor`) merely because a storage page contributed zero visible rows after post-filtering; it MUST advance the cursor to the next storage page while unscanned storage remains. A page MAY carry an empty `matches[]` together with a non-empty `next_cursor`. Early termination would silently truncate results and leak hidden-row boundaries.
- **Registry webhook event profile reserved (RFC-ACDP-0009 §2.10, new).** An OPTIONAL companion profile for registry-to-control-plane lifecycle events (`context_published` / `context_superseded` / `status_changed`) is reserved — distinct from §2.4 push subscriptions (which deliver search matches to discovery consumers). The reserved event shape carries `event_id`, `event_version`, `event_type`, `registry_authority`, `ctx_id`, `lineage_id`, `agent_id`, `visibility`, `derived_from`, and `created_at`; tenant binding is by deployment policy (consistent with §6.4), events SHOULD carry an HMAC-SHA-256 signature header (a registry-to-operator integrity check, NOT the producer signature), and delivery is at-least-once with `event_id`-keyed idempotent processing. Reserved profile name `acdp-registry-events`; not advertisable for conformance in v0.1.0.
- **RFC 8785 numeric conformance vectors (`can-011-jcs-numeric-vectors.json`, new; RFC-ACDP-0001 §5.2).** A new canonicalization fixture pins the RFC 8785 §3.2.2.3 (ECMAScript Number-to-String) behavior that JCS requires: exponential-notation boundaries (magnitude ≥ 1e21 → `e+`, ≤ 1e-7 → `e-`), the plain-decimal band, integer exactness through 2^53, negative-zero normalization, and IEEE 754 magnitude extremes — six executable vectors with byte-exact canonical forms and SHA-256 digests. §5.2's implementer note now cross-references it. Wired into the `acdp-registry-core` and `acdp-consumer` profiles (`registries/profiles.json`, `registries/profiles.md`) and the fixture index (`schemas/conformance/README.md`). The conformance runner executes it automatically; the suite now reports 30 arithmetic vectors.

## v0.1.0 — Clarifications addendum (round 2) — 2026-05-21

**Non-breaking specification clarifications and new conformance fixtures.** The RFCs remain `v0.1.0 Final`; this round tightens three SSRF / wire-convention points found during a second implementation review of the 2026-05-20 addendum. No body field, schema `$id`, JCS rule, content-hash semantic, or signature semantic changed — every existing `v0.1.0` body, signature, and `content_hash` remains valid and wire-compatible. No JSON Schema changed: the schemas already reject the inputs these fixtures pin; the gap was implementation behavior, not schema text.

- **DNS mixed-answer rejection made explicit (RFC-ACDP-0006 §7.1).** When DNS resolution returns multiple addresses and *any* of them is in a forbidden range, the implementation MUST reject the **entire** resolution for that hostname. Silently filtering out the forbidden addresses and connecting to the remaining safe ones ("filter and proceed") is now explicitly non-conformant — an attacker controlling both a public IP and a private target can return both, and a filter-based resolver reaches the private target on a retry, reconnect, or TOCTOU re-resolution. Applies identically to producer DID resolution (§4.8) and DataRef location fetches (§4.9). New fixtures `fed-007`, `did-ssrf-004`, `data-ref-ssrf-004` pin reject-the-whole-resolution behavior in all three fetch contexts.
- **Same-authority defined as scheme + host + effective port (RFC-ACDP-0006 §7.5).** The redirect "same authority" requirement now has a normative definition: two URLs share an authority only when scheme, host (case-insensitive), and effective port (after applying the scheme default — 443 for `https`) all match. An explicit `:443` on an `https://` URL is the same authority as the implicit default; `:8443` is not. A host-only comparison is non-conformant. The definition is cross-referenced from §4.8 and §4.9. New fixtures `fed-008`, `did-ssrf-005`, `data-ref-ssrf-005` pin same-host-different-port redirect rejection.
- **Absent-vs-null convention extended to `DataRef` and more wire types (RFC-ACDP-0002 §6.8, new).** A new subsection states that every `DataRef` optional field (`description`, `size_bytes`, `format`, `schema_version`, `content_hash`, `location`, `embedded`) follows the global absent-vs-null convention (RFC-ACDP-0005 §2.2.1): an unset field MUST be omitted, and an explicit `null` is a `schema_violation`. `location`/`embedded` being mutually exclusive does not make either nullable. New fixtures `schema-011`/`schema-012` pin `DataRef.format`/`DataRef.location` null rejection; `schema-013` pins `error.details`; `schema-014` pins `capabilities.limits.idempotency_key_ttl_seconds`.
- **Server-side authority validation (RFC-ACDP-0002 §3.1).** A registry MUST validate its own authority string before minting `ctx_id` or `origin_registry` values — it MUST be a bare DNS hostname (`acdp-common.schema.json#/$defs/hostname`), never a `host:port`, URL-form, uppercase, or DID-form authority. Libraries SHOULD enforce this at the constructor / configuration-load level so an invalid registry instance cannot be created.
- **Conformance manifest refresh (`registries/profiles.json`, `registries/profiles.md`, `schemas/conformance/README.md`).** The ten new fixtures are wired into the `acdp-registry-core`, `acdp-registry-federated`, and `acdp-consumer` profiles and the fixture index.

## v0.1.0 — Clarifications addendum — 2026-05-20

**Non-breaking specification clarifications and new conformance fixtures.** The RFCs remain `v0.1.0 Final`; this addendum adds normative clarifications and conformance fixtures discovered during implementation review. No body field, schema `$id`, JCS rule, content-hash semantic, or signature semantic changed — every existing `v0.1.0` body, signature, and `content_hash` remains valid and wire-compatible.

- **DataRef location SSRF protection (RFC-ACDP-0008 §4.9, new).** External `data_refs[].location` URLs are producer-controlled — the third attacker-controlled outbound fetch alongside producer DID resolution (§4.8) and cross-registry resolution (RFC-ACDP-0006 §7). A consumer dereferencing a `data_refs[].location` over HTTP(S) MUST apply the same SSRF posture: HTTPS-only, DNS-level IP-range filtering on every resolved address, IP pinning against rebinding, and a same-authority redirect cap. The defense is consumer fetch-time only — a registry does not fetch external `data_refs` and MUST NOT reject a publish on these grounds. New fixtures `data-ref-ssrf-001`/`-002`/`-003` pin IP-literal, DNS-rebinding, and cross-authority-redirect refusal; RFC-ACDP-0002 §6.5 now cross-references §4.9.
- **DNS-level SSRF protection made explicit (RFC-ACDP-0006 §7.1).** URL host-syntax checks are necessary but insufficient: a conformant implementation MUST resolve the hostname, validate every resolved address, and connect only to the validated address(es) without re-resolving. Calling a URL-syntax check and then using an HTTP client that resolves DNS normally is explicitly non-conformant (vulnerable to DNS rebinding and split-horizon DNS). Applies identically to §4.8 and §4.9.
- **Verification profile names (RFC-ACDP-0001 §9.2, new).** RECOMMENDED identifiers for SDK verification modes — `StrictV010` (the only v0.1.0-conformant mode, the §5.11 strict pipeline), `Diagnostic`, and `UnsafeForTests` — so documentation and error messages are consistent across implementations. `Diagnostic` and `UnsafeForTests` MUST NOT be exposed as default behavior. §5.11's strict-policy constructor name is corrected to `strict_v0_1_0()` (it previously read `strict_v0_0_1()`, a stale reference to a version number that never shipped — RFC-ACDP-0001 §1).
- **Consumer-profile SSRF requirement (RFC-ACDP-0001 §9.1, `profiles.json`).** The `acdp-consumer` profile's MUST list and behavioral requirements now state explicitly that consumers apply SSRF protections on every producer-controlled outbound fetch — producer `did:web` resolution (§4.8) and external `data_refs[].location` fetches (§4.9) — matching the fixtures the profile already requires.
- **Hostname vs host:port disambiguation (RFC-ACDP-0002 §3.1).** A normative table clarifying that `origin_registry` and the `ctx_id` authority MUST be a bare DNS hostname with no port, while `did:web` identifiers (`registry_did`, `agent_id`, `signature.key_id`) may carry a percent-encoded port — and that `did:web:<host>%3A<port>` is test/development-only.
- **`acdp_version` SDK default behavior (RFC-ACDP-0001 §6).** Builder-based SDKs SHOULD default to emitting `acdp_version: "0.1.0"` explicitly; an SDK taking the omission default MUST document it, provide a one-line explicit override, and state that absent and explicit `"0.1.0"` are interpreted identically though they are distinct `content_hash` preimages.
- **Conformance manifest refresh (`registries/profiles.json`, `registries/profiles.md`, `schemas/conformance/README.md`).** The three new `data-ref-ssrf-*` fixtures are wired into the `acdp-consumer` profile and the fixture index; a new `data-ref-ssrf` fixture family is registered.
- **Documentation refresh (`docs/`).** `threat-model.md` gains an SSRF row and attack scenario plus the previously-omitted supersession-race row; `integration-guide.md` sets `acdp_version` in the producer example and adds SSRF guidance for DID resolution, DataRef fetches, and cross-registry walks, plus a note that the consumer flow is the `StrictV010` profile; `architecture.md` notes SSRF protection on the cross-registry walk.
- **README / governance / manifesto editorial.** `README.md` repository map corrected — the `registries/` listing now includes `data-ref-types.md` and `profiles.json`, the `examples/` listing includes `key-resolution/`, and the `schemas/conformance/` listing reflects all current fixture families; the `acdp-consumer` profile row and the compatibility-model section are corrected (an inaccurate "embedding models" capability is removed). `governance/GOVERNANCE.md` and `governance/RFC-PROCESS.md` drop the stale "proto" references (ACDP is JSON-only), add `data-ref-types.md` to the registry list, reconcile the maintainers/core-team cross-reference, and add a security-vulnerability reporting path. `manifesto/manifesto.md` gains a closing pointer into the specification.

## v0.1.0 — 2026-05-19

**ACDP is promoted from Release Candidate to `Final`.** RFCs 0001–0008 now carry the `Final` status; RFC-ACDP-0009 remains `Reserved`. `0.1.0` is the first published Final version of the protocol — the `0.0.1` identifier named internal pre-release drafts only and was never promoted. `0.1.0` is wire-compatible with `0.1.0-rc1`: this release is a specification-hardening pass, not a wire change. No body field, schema `$id`, JCS rule, content-hash semantic, or signature semantic changed.

### Specification hardening since `0.1.0-rc1`

- **DataRef schema openness made explicit (RFC-ACDP-0002 §6.7, `acdp-data-ref.schema.json`).** The `DataRef` root object is now an explicit open schema (`additionalProperties: true`): root-level `DataRef` fields are producer-controlled and included in the `content_hash` preimage, so consumers MUST preserve unknown `DataRef` fields when recomputing the hash. The nested `embedded` sub-object remains closed. New fixture `can-010` pins the hash recomputation; the openness map in RFC-ACDP-0007 §3.3.1 gains a `DataRef` root row.
- **Absent-vs-null wire convention (RFC-ACDP-0005 §2.2.1).** Optional fields MUST be omitted, never serialized as JSON `null`, unless the schema explicitly types them nullable (`supersedes` is the one explicitly-nullable example). New fixtures `schema-005`/`schema-006`/`schema-007` pin rejection of `next_cursor: null`, `summary: null`, `domain: null`.
- **Producer DID resolution SSRF protection (RFC-ACDP-0008 §4.8).** Registries and consumers MUST apply the RFC-ACDP-0006 §7 SSRF defenses to producer `did:web` resolution, not only to cross-registry resolution. New fixtures `did-ssrf-001`/`did-ssrf-002`/`did-ssrf-003` pin loopback, IMDS, and private-range refusal; the `key_resolution_failed` code is broadened to cover SSRF-policy refusal.
- **Closed nested-schema conformance (RFC-ACDP-0007 §3.3.1).** New fixtures `schema-008`/`schema-009`/`schema-010` pin rejection of unknown fields in the closed `signature`, `data_period`, and `capabilities.limits` sub-objects.
- **`data_ref_hash_mismatch` SDK guidance (RFC-ACDP-0007 §5.3).** New guidance for SDK authors on keeping `data_ref_hash_mismatch` distinct from `hash_mismatch` and `invalid_signature`. New fixture `data-ref-008` pins the consumer-side external-DataRef mismatch case (body stays cryptographically valid).
- **Cursor conformance fixtures (RFC-ACDP-0005 §2.5.4).** New fixtures `cur-001` (`cursor_expired`) and `cur-002` (`invalid_cursor`).
- **`lineage_id` derivation golden vector (RFC-ACDP-0001 §5.6).** New dedicated fixture `lin-001`; the conformance runner now executes `lin-*` fixtures alongside `can-*`.
- **`acdp_version` producer guidance (RFC-ACDP-0001 §6).** Producers SHOULD set `acdp_version` explicitly; consumers, verifiers, and registries MUST NOT inject a default `acdp_version` into a body before recomputing `content_hash`.
- **Conformance manifest refresh (`registries/profiles.json`, `registries/profiles.md`, `schemas/conformance/README.md`).** All 14 new fixtures are wired into the `acdp-registry-core`, `acdp-registry-discovery`, and `acdp-consumer` profiles and the fixture index.
- **Editorial.** RFC headers, the RFC index, `README.md`, and `VERSIONING.md` updated from `Release Candidate 1` to `Final`. A `RELEASE.md` promotion checklist was added.

## v0.1.0-rc1 — 2026-05-18

**First published version of the Agent Context Distribution Protocol (ACDP), entering the world as Release Candidate 1.** RFCs 0001–0008 are at status `Release Candidate 1`; RFC-ACDP-0009 remains `Reserved`. The `0.0.1` identifier used by earlier pre-release drafts was never promoted to a Release Candidate or Final status and is superseded by `0.1.0`. `0.1.0` is wire-compatible with those drafts — the body format, JCS canonicalization, content-hash, and signature semantics are unchanged.

ACDP defines the minimal substrate for autonomous agents to publish, discover, and verify units of contextual information across distributed systems. It is JSON-only, coordination-agnostic, and has no central authority.

### Release Candidate scope

`0.1.0-rc1` is intended for implementation testing. Backward-incompatible changes remain possible until `Final`; only editorial fixes are expected during the RC window. Per [VERSIONING.md](VERSIONING.md), `0.1.0` is promoted to `Final` once the conformance suite — including the behavioral fixtures under `schemas/conformance/` — passes against at least two interoperating implementations.

### Normative RFCs (Release Candidate 1)

- **RFC-ACDP-0001 — Core.** Identifiers (`acdp://`, `lin:sha256:`), JCS canonicalization (RFC 8785), content hashing (`sha256:<hex>`), Ed25519 signatures, time format, `did:web` resolution, the v0.1.0 strict verification profile, and the **Body / ProducerContent / RegistryState** terminology that the rest of the spec depends on.
- **RFC-ACDP-0002 — Context Body.** The immutable signed body schema: context types, `data_refs` (URL / inline / embedded), visibility (`public` / `restricted` / `private`), `derived_from`, metadata bounds. `origin_registry` is normatively a DNS hostname, not a DID URI.
- **RFC-ACDP-0003 — Publish & Supersession.** `POST /contexts`, registry-assigned fields (`ctx_id`, `lineage_id`, `origin_registry`, `created_at`), the ordered publish-validation algorithm, supersession constraints, and `Idempotency-Key` semantics.
- **RFC-ACDP-0004 — Retrieval & Lineage.** `GET /contexts/{ctx_id}`, body-only retrieval, lineage queries (including `/current` semantics and lineage-endpoint visibility), visibility-aware cache headers, registry-attested status.
- **RFC-ACDP-0005 — Discovery.** Keyword search across the seven required fields, opaque cursors with ≥1h validity, requester-scoped pagination, `anonymous_public_reads` scoping, and whole-result-set `total_estimate`. Ranking is registry-defined.
- **RFC-ACDP-0006 — Cross-Registry References.** `acdp://` URI scheme, resolution flow, registry-DID-vs-producer-signature distinction, SSRF defenses.
- **RFC-ACDP-0007 — Capabilities & Errors.** `/.well-known/acdp.json` capability declaration, the standard error envelope, and the error-code registry (including `data_ref_hash_mismatch`).
- **RFC-ACDP-0008 — Security & Threat Model.** A2A threat model, required defenses, visibility enforcement, key-rotation rules, replay prevention.

### Reserved (numbering pinned, no normative text)

- **RFC-ACDP-0009 — Extensions.** Reserves the field namespaces and feature areas planned for future versions: retraction / lifecycle events, post-publication relationships, attestations, push subscriptions, server-side traversal (`/walk`), semantic similarity, cross-registry supersession, registry receipts.

### Conformance and tooling

- **11 canonical JSON Schemas** under `schemas/json/` covering context, body, registry-state, data-ref, publish request/response, search response, capabilities, the error envelope, common types, and the index. Schemas are namespaced under `schemas.acdp.io/v0.1.0/`.
- **Conformance fixtures** under `schemas/conformance/`: JCS canonicalization vectors, cryptographic golden vectors, and behavioral scenarios (publish, retrieval, lineage, visibility, capabilities, error-envelope, DataRef, metadata, body identity-field, and idempotency cases).
- **Conformance runner** (`scripts/conformance-runner.py`) that executes the arithmetic and cryptographic vectors end-to-end. Behavioral fixtures (`pub-*`, `vis-*`, `ret-*`, `body-*`, `err-*`, …) require a live registry to execute.
- **Open-vocabulary registries** under `registries/`: `auth-methods`, `context-types`, `error-codes`, `locator-schemes`, `media-types`, `profiles`, `signature-algorithms`. Each entry carries a status (`Proposed` / `Provisional` / `Stable` / `Deprecated`).
- **GitHub Actions CI** that validates JSON Schemas, examples, and conformance vectors on every PR.
- **`make bootstrap` + `make validate`** for one-shot local setup and verification.

### Conformance profiles

- `acdp-registry-core` *(default)* — RFC-0001..0004, 0007, 0008. Every conformant registry.
- `acdp-registry-discovery` — adds RFC-0005 keyword search.
- `acdp-registry-federated` — adds RFC-0006 cross-registry resolution.
- `acdp-consumer` — RFC-0001 + RFC-0004 (read) + RFC-0006.

### Examples

Working examples under `examples/` for capabilities, error envelopes, idempotency cycles, lineage walks, mixed `data_refs`, publishing, retrieval (including a real Ed25519 round-trip in `retrieval/golden-context.json`), keyword-search responses, supersession (v2 superseding v1), and visibility (`restricted` / `private` bodies). Every example is validated against the canonical schemas in CI.

### Hardening incorporated since the pre-release drafts

`0.1.0-rc1` folds in several rounds of specification hardening over the pre-release drafts, including: explicit lineage-endpoint visibility rules; `GET /lineages/{id}/current` semantics for all-superseded and expired-head lineages; `anonymous_public_reads` scoping of keyword search; whole-result-set `total_estimate`; the `data_ref_hash_mismatch` error code distinguishing DataRef-level integrity failures from body-level ones; a normative `origin_registry`-is-a-hostname rule with schema enforcement; and the v0.1.0 strict verification profile with recommended verification-report stage names.

### Explicitly out of scope in v0.1.0

Retraction (any form), post-publication `builds_on` relationships from third parties, attestations (`reproduced` / `disputes`), push subscriptions, server-side traversal, federation peering, cross-registry query forwarding, encrypted bodies, hard deletion, multi-party / threshold signatures, semantic similarity, and registry quality / reputation algorithms. Most of these are reserved in RFC-ACDP-0009 for future versions.
