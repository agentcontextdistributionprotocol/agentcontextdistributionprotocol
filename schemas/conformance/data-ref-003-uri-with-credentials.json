{
  "id": "data-ref-003",
  "description": "DataRef.location URI contains userinfo credentials (user:password@host). Registry MUST reject with schema_violation. Producers MUST NOT embed secrets in `data_refs[].location` because the value is persisted in the signed body forever.",
  "tags": ["failure", "publish", "data-refs", "security"],
  "input": {
    "endpoint": "POST /contexts",
    "data_ref_under_test": {
      "type": "raw_data",
      "location": "postgres://admin:hunter2@db.example.com/sales"
    }
  },
  "expected": {
    "outcome": "failure",
    "http_status": 400,
    "error_code": "schema_violation",
    "checklist_step": 4,
    "rationale": "Even if the runtime accepted credentials in the URI, the body is signed and immutable; rotating the password later does not undo the leak."
  }
}
